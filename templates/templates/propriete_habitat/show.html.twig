{% extends 'base.html.twig' %}

{% block title %}ProprieteHabitat{% endblock %}

{% block body %}
    <h1>ProprieteHabitat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ propriete_habitat.id }}</td>
            </tr>
            <tr>
                <th>ValeurPropriete</th>
                <td>{{ propriete_habitat.valeurPropriete }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('propriete_habitat_index') }}">back to list</a>

    <a href="{{ path('propriete_habitat_edit', {'id': propriete_habitat.id}) }}">edit</a>

    {{ include('propriete_habitat/_delete_form.html.twig') }}
{% endblock %}
