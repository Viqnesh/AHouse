<form method="post" action="{{ path('activite_habitat_delete', {'id': activite_habitat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite_habitat.id) }}">
    <button class="btn">Delete</button>
</form>
