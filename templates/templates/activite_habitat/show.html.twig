{% extends 'base.html.twig' %}

{% block title %}ActiviteHabitat{% endblock %}

{% block body %}
    <h1>ActiviteHabitat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite_habitat.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ activite_habitat.adresse }}</td>
            </tr>
            <tr>
                <th>Distance</th>
                <td>{{ activite_habitat.distance }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ activite_habitat.prix }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ activite_habitat.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activite_habitat_index') }}">back to list</a>

    <a href="{{ path('activite_habitat_edit', {'id': activite_habitat.id}) }}">edit</a>

    {{ include('activite_habitat/_delete_form.html.twig') }}
{% endblock %}
