{% extends 'base.html.twig' %}

{% block title %}Domaine{% endblock %}

{% block body %}
    <h1>Domaine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaine.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ domaine.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ domaine.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ domaine.image }}</td>
            </tr>
            <tr>
                <th>Arrive</th>
                <td>{{ domaine.arrive ? domaine.arrive|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Depart</th>
                <td>{{ domaine.depart ? domaine.depart|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ domaine.adresse }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ domaine.ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ domaine.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('domaine_index') }}">back to list</a>

    <a href="{{ path('domaine_edit', {'id': domaine.id}) }}">edit</a>

    {{ include('domaine/_delete_form.html.twig') }}
{% endblock %}
